# Leave these unset to use Google's test ad units.
# Production values belong in a local .env, which is not committed.
# Also set admob.app.id in android/local.properties to the production app id.
VITE_ADMOB_BANNER_ID=
VITE_ADMOB_INTERSTITIAL_ID=
